import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {flexDirection: 'row', alignItems: 'flex-end', left: 10},
  badge: {position: 'absolute', right: 3, bottom: 4},
});
