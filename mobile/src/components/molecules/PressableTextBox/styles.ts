import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  outline: {
    borderWidth: 1,
    borderColor: '#585858',
    width: '100%',
    height: 150,
    borderRadius: 5,
    overflow: 'hidden',
    alignContent: 'center',
  },
});
