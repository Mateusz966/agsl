import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  outline: {
    marginVertical: 10,
    borderWidth: 1,
    borderColor: '#585858',
    width: '100%',
    height: '30%',
    borderRadius: 5,
    overflow: 'hidden',
    alignContent: 'center',
  },
});
